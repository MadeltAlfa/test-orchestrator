# Drop-in Nginx. Wajib jika deploy di balik Nginx, tanpa ini Nginx tolak body besar
# sebelum PHP sempat proses (413 Request Entity Too Large).
# Deploy: cp ke /etc/nginx/conf.d/uploads.conf lalu nginx -t && systemctl reload nginx.

client_max_body_size 512m;
